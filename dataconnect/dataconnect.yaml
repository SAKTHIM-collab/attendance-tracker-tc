specVersion: "v1"
serviceId: "attendance-tracker-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "attendance-tracker-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
